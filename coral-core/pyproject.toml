[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "coral-core"
version = "0.0.0"
description = "The shared contract for the coral plugin system."
requires-python = ">=3.12"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/coral_core"]
