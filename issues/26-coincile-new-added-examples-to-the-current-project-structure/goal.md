# `pypde` in new plugin structure

## Situation

In last three commit of the current branch `pypde-plugin` the author Michele Matteucci added `pypde` plugin to the project.
This addition was made *before* the refactoring of plugin system.

## Goals

* The main goal is to integrate the plugin added in `pypde-plugin` to the current plugin system. The branch `26-*` is the feature branch for this addition. The structure of the new plugin should be *very simple*, *pedagocial*, *coincise*.
* Remove unused files/directories, conforming to `main` branch.
* Place examples in the right folder.
* Possibly write tests.

## To Do

1. investigate on `pypde` plugin;
2. let us take all the decision together;
3. write a plan to implement; what we have to implement.

## Reccommendations

* Be extremely brief and concise both when asking stuff and in the code/docs.
* Every decision must be taken together.
* Do not do what you are not told to do.
